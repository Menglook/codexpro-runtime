import { OFFICE_SCENE_SPRITE_SCRIPT } from "./officeSceneSprites.js";
import { OFFICE_SCENE_LAYOUT_HTML, OFFICE_SCENE_LAYOUT_LITERAL } from "./officeSceneLayout.js";
import { OFFICE_AGENT_SPRITE_DEFINITIONS } from "./officeSceneAgentAssets.js";
import { OFFICE_SCENE_ENVIRONMENT_HTML } from "./officeSceneEnvironment.js";
import { OFFICE_SCENE_PROCESS_RUNTIME_SCRIPT } from "./officeSceneProcessRuntime.js";
import { OFFICE_SCENE_EXPERIENCE_RUNTIME_SCRIPT } from "./officeSceneExperienceRuntime.js";

export interface OfficeSceneFeatureFlag {
  enabled: boolean;
  projects: string[];
}

function booleanFlag(value: string | undefined): boolean | null {
  if (value === undefined || value.trim() === "") return null;
  const normalized = value.trim().toLowerCase();
  if (["1", "true", "yes", "on", "all"].includes(normalized)) return true;
  if (["0", "false", "no", "off", "none"].includes(normalized)) return false;
  return null;
}

export function officeSceneFeatureFlag(env: NodeJS.ProcessEnv = process.env): OfficeSceneFeatureFlag {
  const explicit = booleanFlag(env.CODEXPRO_OFFICE_SCENE_ADVANCED) ?? booleanFlag(env.CODEXPRO_OFFICE_SCENE);
  const projects = (env.CODEXPRO_OFFICE_SCENE_ADVANCED_PROJECTS ?? env.CODEXPRO_OFFICE_SCENE_PROJECTS ?? "")
    .split(/[;,\n]/u)
    .map((value) => value.trim())
    .filter(Boolean);
  return {
    enabled: explicit === true,
    projects: [...new Set(projects)]
  };
}

export const OFFICE_SCENE_SURFACE_HTML = String.raw`
    <div class="office-view-switch" id="officeViewSwitch" aria-label="办公室视图">
      <button type="button" data-office-view="scene">智能体办公室</button>
      <button type="button" data-office-view="board">状态看板</button>
    </div>
  </section>
  <section class="office-scene-shell" id="officeSceneShell" data-office-scene data-scene-version="ova-1" aria-label="高级科技智能体办公室">
    ${OFFICE_AGENT_SPRITE_DEFINITIONS}
    <div class="office-scene-card">
      <div class="office-scene-head"><div><h2 id="officeSceneTitle">科技智能体办公室</h2><div class="sub" id="officeSceneSubtitle">只显示有真实执行依据的智能体</div></div><label class="scene-focus">重点区域<select id="officeSceneZoneFocus"></select></label><div class="scene-experience-controls" aria-label="动画模式"><button type="button" id="officeSceneLiveMode" class="active">实时</button><button type="button" id="officeSceneHistoryMode">历史回放</button><button type="button" id="officeHistoryPause" hidden>暂停回放</button><button type="button" id="officeHistoryExit" hidden>退出回放</button><span id="officeHistoryBadge" hidden></span></div><div class="scene-status" id="officeSceneStatus" role="status" aria-live="polite"></div><details class="scene-animation-diagnostics technical-only" id="officeAnimationDiagnostics"><summary>动画诊断</summary><div id="officeAnimationDiagnosticsBody">尚未启动</div></details></div>
      <div class="office-scene-viewport" id="officeSceneViewport">
        <div class="office-scene-camera">
          <div class="office-scene-canvas" id="officeSceneCanvas">
${OFFICE_SCENE_ENVIRONMENT_HTML}
${OFFICE_SCENE_LAYOUT_HTML}
            <svg class="scene-relation-layer" id="officeRelationLayer" viewBox="0 0 1000 700" aria-label="已验证任务关系"></svg>
            <div class="scene-actor-layer" id="officeActorLayer"></div>
            <div class="scene-empty" id="officeSceneEmpty">当前筛选没有可证明的活跃智能体；状态看板和任务回报仍可使用。</div>
          </div>
        </div>
      </div>
      <section class="scene-live-feed" aria-label="重要任务实时动态"><div class="scene-live-feed-head"><b>实时动态</b><span>仅显示重要进展</span></div><div class="scene-live-feed-items" id="officeSceneLiveFeed" role="log" aria-live="polite" aria-relevant="additions"><span class="scene-live-feed-empty">等待新的重要进展…</span></div></section>
    </div>
  </section>
  <section id="officeBoardBoundary">
`;

export const OFFICE_SCENE_SURFACE_SCRIPT = String.raw`
${OFFICE_SCENE_SPRITE_SCRIPT}
${OFFICE_SCENE_EXPERIENCE_RUNTIME_SCRIPT}
${OFFICE_SCENE_PROCESS_RUNTIME_SCRIPT}
      const OFFICE_SCENE_LAYOUT = ${OFFICE_SCENE_LAYOUT_LITERAL};
      function initializeOfficeSceneState(){return {feature:null,view:"board",actors:new Map(),devices:new Map(),occupancy:new Map(),movements:new Map(),movementSequence:0,reportCursors:new Map(),reportControllers:new Map(),eventTimers:new Map(),feedKeys:[],listenerCleanups:[],listenersInstalled:false,focusZone:null,selectedActorId:null,projectId:null,reducedMotion:window.matchMedia("(prefers-reduced-motion: reduce)").matches};}
      function configureOfficeSceneFeature(flag){state.scene.feature={enabled:flag.enabled===true,projects:new Set(Array.isArray(flag.projects)?flag.projects:[])};}
      function officeSceneFeatureEnabled(project){const feature=state.scene.feature;if(!feature||!project)return false;if(feature.enabled)return true;return [project.project_id,project.name,project.root].some((value)=>value&&feature.projects.has(value));}
      function updateSceneActorNode(node,actor,options={}){node.dataset.actorId=actor.actor_id;node.dataset.executorId=actor.executor_id;node.dataset.objectiveKey=actor.objective_key;node.dataset.pose=options.pose||actor.pose;node.dataset.zone=actor.zone_id;node.dataset.stationId=actor.station_id;node.dataset.prototype=actor.identity?.prototype||"developer";node.dataset.facing=actor.identity?.facing||"right";node.style.setProperty("--agent-core",actor.identity?.core_color||"#35d7ff");node.style.setProperty("--agent-sensor",actor.identity?.sensor_color||"#9af4ff");node.style.setProperty("--agent-delay","-"+Number(actor.identity?.motion_offset_ms||0)+"ms");if(options.position!==false){node.style.left=(actor.x/OFFICE_SCENE_LAYOUT.width*100)+"%";node.style.top=(actor.y/OFFICE_SCENE_LAYOUT.height*100)+"%";}node.style.zIndex=String(actor.z_index);node.setAttribute("aria-label",actor.label+" · "+(actor.action||"当前状态已同步"));node.setAttribute("aria-pressed",String(state.scene.selectedActorId===actor.actor_id));let bubble=node.querySelector(".actor-bubble");if(actor.action){if(!bubble){bubble=document.createElement("span");bubble.className="actor-bubble";node.prepend(bubble);}bubble.textContent=String(actor.action).slice(0,120);}else bubble?.remove();const name=node.querySelector(".actor-name");if(name)name.textContent=actor.label;const plate=node.querySelector(".actor-plate");if(plate)plate.textContent=(actor.identity?.badge||"AI")+" · "+(actor.identity?.tool_module||"智能体");const device=actor.browser_active?'▣':actor.validation_active?'✓':actor.writer_active?'✎':'';let badge=node.querySelector(".actor-device-badge");if(device){if(!badge){badge=document.createElement("span");badge.className="actor-device-badge";node.append(badge);}badge.textContent=device;}else badge?.remove();}
      function sceneActorNode(actor){const node=document.createElement("button");node.type="button";node.className="office-actor";node.innerHTML=officeAgentSvg(actor.identity)+'<span class="actor-name"></span><span class="actor-plate"></span>';updateSceneActorNode(node,actor);node.addEventListener("click",(event)=>{const bubble=event.target.closest?.(".actor-event-bubble"),objective=findObjective(node.dataset.objectiveKey);if(bubble&&objective){openReportCenter(objective,Number(bubble.dataset.sequence),bubble.dataset.reportTab);return;}state.scene.selectedActorId=node.dataset.actorId||null;for(const entry of state.scene.actors.values())entry.node.setAttribute("aria-pressed",String(entry.node===node));$("officeSceneShell")?.classList.add("route-visible");openDrawer(node.dataset.objectiveKey,true);});return node;}
      function sceneTeamSummaryNode(zone,count){const bounds=OFFICE_SCENE_LAYOUT.zones.find((item)=>item.zone_id===zone)||OFFICE_SCENE_LAYOUT.zones.find((item)=>item.zone_id==="archive");const node=document.createElement("div");node.className="scene-team-summary";node.dataset.summaryZone=zone;node.setAttribute("role","status");node.setAttribute("aria-label","该区域另有 "+count+" 个智能体，可在任务抽屉查看");node.style.left=((bounds.x+bounds.width/2)/OFFICE_SCENE_LAYOUT.width*100)+"%";node.style.top=((bounds.y+bounds.height-24)/OFFICE_SCENE_LAYOUT.height*100)+"%";node.textContent="另有 "+count+" 个智能体";return node;}
      function sceneVisibleActors(allActors,mobile){if(!mobile)return allActors.slice(0,12);const selected=[],seen=new Set();for(const zone of ["waiting_user","incident","writer"]){const actor=allActors.find((item)=>item.zone_id===zone&&!seen.has(item.actor_id));if(actor){selected.push(actor);seen.add(actor.actor_id);}}for(const actor of allActors)if(selected.length<4&&!seen.has(actor.actor_id)){selected.push(actor);seen.add(actor.actor_id);}return selected;}
      function populateSceneFocus(){const select=$("officeSceneZoneFocus");if(!select||select.options.length)return;for(const zone of OFFICE_SCENE_LAYOUT.zones){const option=document.createElement("option");option.value=zone.zone_id;option.textContent=zone.label;select.append(option);}}
      function applySceneMobileFocus(actors=[]){const viewport=$("officeSceneViewport"),canvas=$("officeSceneCanvas"),select=$("officeSceneZoneFocus");if(!viewport||!canvas||!select)return;const mobile=window.matchMedia("(max-width: 720px)").matches;if(!mobile){canvas.style.removeProperty("width");canvas.style.removeProperty("height");canvas.style.removeProperty("transform");return;}const preferred=state.scene.focusZone||actors[0]?.zone_id||"development",zone=OFFICE_SCENE_LAYOUT.zones.find((item)=>item.zone_id===preferred)||OFFICE_SCENE_LAYOUT.zones.find((item)=>item.zone_id==="development");state.scene.focusZone=zone.zone_id;select.value=zone.zone_id;const width=viewport.clientWidth||320,height=viewport.clientHeight||420,scale=Math.min((width-20)/zone.width,(height-20)/zone.height),left=(width-zone.width*scale)/2-zone.x*scale,top=(height-zone.height*scale)/2-zone.y*scale;canvas.style.width=OFFICE_SCENE_LAYOUT.width+"px";canvas.style.height=OFFICE_SCENE_LAYOUT.height+"px";canvas.style.transform="translate("+left+"px,"+top+"px) scale("+scale+")";}
      function sceneRouteFromNode(fromNodeId,toStationId){const nodes=new Map(OFFICE_SCENE_LAYOUT.route_nodes.map((node)=>[node.node_id,node]));if(!nodes.has(fromNodeId)||!nodes.has(toStationId))return [];const queue=[fromNodeId],previous=new Map([[fromNodeId,null]]);while(queue.length){const current=queue.shift();if(current===toStationId)break;for(const next of nodes.get(current)?.neighbors||[])if(nodes.has(next)&&!previous.has(next)){previous.set(next,current);queue.push(next);}}if(!previous.has(toStationId))return [];const route=[];for(let cursor=toStationId;cursor;cursor=previous.get(cursor)||null)route.unshift(nodes.get(cursor));return route;}
      function sceneCurrentPoint(node){const canvas=$("officeSceneCanvas");if(!canvas?.clientWidth||!canvas.clientHeight)return null;const style=getComputedStyle(node),left=Number.parseFloat(style.left),top=Number.parseFloat(style.top);if(!Number.isFinite(left)||!Number.isFinite(top))return null;return {x:left/canvas.clientWidth*OFFICE_SCENE_LAYOUT.width,y:top/canvas.clientHeight*OFFICE_SCENE_LAYOUT.height};}
      function sceneNearestRouteNode(point){let selected=null,distance=Infinity;for(const node of OFFICE_SCENE_LAYOUT.route_nodes){const next=(node.x-point.x)**2+(node.y-point.y)**2;if(next<distance){selected=node;distance=next;}}return selected;}
      function cancelSceneMovement(actorId,settle=true){const movement=state.scene.movements.get(actorId);if(!movement)return;state.scene.movements.delete(actorId);movement.animation.cancel();const entry=state.scene.actors.get(actorId);if(settle&&entry)updateSceneActorNode(entry.node,movement.targetActor);if(state.scene.movements.size===0)$("officeSceneShell")?.classList.remove("moving");}
      function cancelAllSceneMovements(settle=true){for(const actorId of [...state.scene.movements.keys()])cancelSceneMovement(actorId,settle);}
      function moveSceneActor(entry,target,mobile){const actorId=target.actor_id,active=state.scene.movements.get(actorId);if(active?.targetActor.station_id===target.station_id){active.targetActor=target;updateSceneActorNode(entry.node,target,{position:false,pose:"walking"});entry.actor=target;return true;}const activeLimit=mobile?2:6;if(!active&&state.scene.movements.size>=activeLimit)return false;const currentPoint=sceneCurrentPoint(entry.node)||{x:entry.actor.x,y:entry.actor.y};if(active)cancelSceneMovement(actorId,false);const nearest=sceneNearestRouteNode(currentPoint),route=nearest?sceneRouteFromNode(nearest.node_id,target.station_id):[];if(route.length<1||typeof entry.node.animate!=="function")return false;const points=[currentPoint,...route.map((node)=>({x:node.x,y:node.y}))];const keyframes=points.map((point)=>({left:(point.x/OFFICE_SCENE_LAYOUT.width*100)+"%",top:(point.y/OFFICE_SCENE_LAYOUT.height*100)+"%"}));updateSceneActorNode(entry.node,target,{pose:"walking"});const duration=Math.min(1600,Math.max(420,260+points.length*150)),token=++state.scene.movementSequence;const animation=entry.node.animate(keyframes,{duration,easing:"linear",iterations:1,fill:"forwards"});const movement={animation,targetActor:target,token};state.scene.movements.set(actorId,movement);$("officeSceneShell")?.classList.add("moving");entry.actor=target;animation.finished.then(()=>{if(state.scene.movements.get(actorId)?.token!==token)return;state.scene.movements.delete(actorId);updateSceneActorNode(entry.node,movement.targetActor);const pendingPose=entry.node.dataset.pendingProcessPose;if(pendingPose&&entry.node.classList.contains("process-active"))entry.node.dataset.pose=pendingPose;delete entry.node.dataset.pendingProcessPose;if(state.scene.movements.size===0)$("officeSceneShell")?.classList.remove("moving");}).catch(()=>undefined);return true;}
      const OFFICE_SCENE_IMPORTANT_REPORTS=new Set(["stage_completed","finding","warning","validation_passed","validation_failed","git_committed","git_pushed","git_failed","task_completed","task_failed","task_cancelled","assistant_progress","assistant_summary"]);
      function sceneReportIdentity(projectId,taskId){return String(projectId)+"\u0000"+String(taskId);}
      function sceneReportTab(kind){if(["finding","warning","assistant_summary"].includes(kind))return "findings";if(["validation_started","validation_passed","validation_failed"].includes(kind))return "acceptance";if(["artifact_created","git_committed","git_pushed","git_failed"].includes(kind))return "delivery";return "progress";}
      function sceneReportText(event){const raw=String(event.text||event.summary||event.title||"").replaceAll(String.fromCharCode(96)," ").replace(/[*_>#\[\]()]/g," ").replace(/[\u0000-\u001f\u007f]+/g," ").replace(/\s+/g," ").trim();if(/[\u3400-\u9fff]/u.test(raw))return raw.slice(0,96);const labels={stage_completed:"当前阶段已经完成",finding:"发现一项重要信息",warning:"发现一项需要关注的问题",validation_passed:"测试验收已经通过",validation_failed:"测试验收未通过",git_committed:"代码已经本地提交",git_pushed:"代码已经同步到远端",git_failed:"代码同步未成功",task_completed:"任务已经完成",task_failed:"任务执行失败",task_cancelled:"任务已经取消",assistant_progress:"ChatGPT 已发布一条公开进度",assistant_summary:"智能体已生成最新总结"};return labels[event.event_kind]||"任务有一条新的重要进展";}
      function showSceneReportEvent(actorId,event){if(officeProcessEnabled()&&officeProcessAnimationFeature.progress_bubbles)return;const entry=state.scene.actors.get(actorId);if(!entry)return;clearTimeout(state.scene.eventTimers.get(actorId));entry.node.querySelector(".actor-event-bubble")?.remove();const text=sceneReportText(event),bubble=document.createElement("span");bubble.className="actor-event-bubble";bubble.dataset.sequence=String(event.sequence);bubble.dataset.reportTab=sceneReportTab(event.event_kind);bubble.dataset.severity=event.severity||"info";bubble.textContent=text;entry.node.prepend(bubble);const timer=setTimeout(()=>{if(state.scene.eventTimers.get(actorId)!==timer)return;state.scene.eventTimers.delete(actorId);bubble.remove();},6000);state.scene.eventTimers.set(actorId,timer);const feed=$("officeSceneLiveFeed"),feedKey=String(event.feed_key||String(event.event_kind)+"\u0000"+text);if(feed&&!state.scene.feedKeys.includes(feedKey)){feed.querySelector(".scene-live-feed-empty")?.remove();const item=document.createElement("button");item.type="button";item.className="scene-live-feed-item";item.dataset.severity=event.severity||"info";item.innerHTML='<span class="scene-live-feed-dot"></span><span>'+escapeHtml(text)+'</span><time>'+escapeHtml(new Date(event.occurred_at||Date.now()).toLocaleTimeString("zh-CN",{hour:"2-digit",minute:"2-digit"}))+'</time>';item.addEventListener("click",()=>{const objective=findObjective(entry.node.dataset.objectiveKey);if(objective)openReportCenter(objective,Number(event.sequence),sceneReportTab(event.event_kind));});feed.prepend(item);state.scene.feedKeys.unshift(feedKey);while(feed.children.length>6)feed.lastElementChild?.remove();if(state.scene.feedKeys.length>20)state.scene.feedKeys.length=20;}}
      function syncOfficeSceneReports(data,visualProject,visibleActors){if(!visualProject)return;const project=data.projects?.find((item)=>item.project_id===visualProject.project_id);if(!project||!reportFeatureEnabled(project))return;const actors=new Map((visibleActors||[]).map((actor)=>[actor.task_id,actor]));const events=(Array.isArray(data.activity_feed)?data.activity_feed:[]).filter((event)=>event.project_id===project.project_id&&actors.has(event.task_id)).sort((left,right)=>Date.parse(left.occurred_at||0)-Date.parse(right.occurred_at||0)||Number(left.sequence||0)-Number(right.sequence||0));for(const event of events){if(state.scene.feedKeys.includes(String(event.feed_key||"")))continue;const actor=actors.get(event.task_id);if(actor)showSceneReportEvent(actor.actor_id,event);}}
      function setOfficeView(view,persist=true){const shell=$("officeSceneShell"),switcher=$("officeViewSwitch"),board=$("officeBoardBoundary");if(!shell||!switcher||!board)return;const next=view==="scene"&&switcher.classList.contains("available")?"scene":"board";state.scene.view=next;shell.classList.toggle("active",next==="scene");board.classList.toggle("state-board-hidden",next==="scene");for(const button of switcher.querySelectorAll("[data-office-view]")){const active=button.dataset.officeView===next;button.classList.toggle("active",active);button.setAttribute("aria-pressed",String(active));}if(persist)try{localStorage.setItem("codexpro.office.view",next);}catch{}}
      function reconcileSceneDevices(visualProject){const devices=new Map((visualProject?.devices||[]).map((device)=>[device.device_id,device]));state.scene.devices=devices;const bindings={"browser-device":{kind:"browser",label:"浏览器操作"},"acceptance-device":{kind:"acceptance",label:"测试验收"},"writer-device":{kind:"writer_lease",label:"文件写入"},"diagnostic-device":{zone:"incident",label:"异常处理"},"recovery-device":{zone:"recovering",label:"恢复处理"},"dispatch-device":{zone:"dispatch",label:"任务分派"},"queue-device":{zone:"writer_queue",label:"写入排队"},"delivery-device":{zone:"delivery",label:"代码交付"},"archive-device":{zone:"archive",label:"完成归档"}};const actors=[...(visualProject?.actors||[])];for(const [furnitureId,binding] of Object.entries(bindings)){const node=document.querySelector('[data-furniture-id="'+furnitureId+'"]');if(!node)continue;const device=binding.kind?[...devices.values()].find((item)=>item.device_kind===binding.kind&&item.state==="active"):null,actor=binding.zone?actors.find((item)=>item.zone_id===binding.zone):null,active=device||actor;node.dataset.authoritativeState=active?"active":"inactive";if(!node.dataset.processStage)node.dataset.state=node.dataset.authoritativeState;node.dataset.objectiveKey=active?.objective_key||"";node.title=active?(device?.details||binding.label+"正在进行"):binding.label+"当前没有可证明的活动";node.tabIndex=0;node.setAttribute("role","button");}}
      function updateOfficeScene(data,mode,direct=false){const shell=$("officeSceneShell"),switcher=$("officeViewSwitch"),layer=$("officeActorLayer"),empty=$("officeSceneEmpty");if(!shell||!switcher||!layer||!empty)return;const project=mode==="project"?data.projects?.[0]:null;const available=Boolean(project&&officeSceneFeatureEnabled(project));switcher.classList.toggle("available",available);if(!available){destroyOfficeScene();setOfficeView("board",false);return;}if(state.scene.projectId&&state.scene.projectId!==project.project_id)destroyOfficeScene();state.scene.projectId=project.project_id;$("officeSceneTitle").textContent=project.name+" · 科技智能体办公室";const visualSnapshot=data.visual_snapshot,visualMatches=Boolean(visualSnapshot&&visualSnapshot.projection_revision===data.revision&&visualSnapshot.projection_id===data.projection_id),visualProject=visualMatches?visualSnapshot.projects?.find((item)=>item.project_id===project.project_id&&item.projection_revision===data.revision):null;$("officeSceneSubtitle").textContent=visualProject?"智能体、设备和动作均来自当前真实任务状态":visualSnapshot&&!visualMatches?"动画与看板版本不一致，已保持安全空场景":"当前视觉状态暂时不可用，已保持安全空场景";const mobile=window.matchMedia("(max-width: 720px)").matches;const allActors=Array.isArray(visualProject?.actors)?visualProject.actors:[];const actors=sceneVisibleActors(allActors,mobile);const visibleIds=new Set(actors.map((actor)=>actor.actor_id)),folded=new Map(Object.entries(visualProject?.folded_actor_counts||{}));for(const actor of allActors)if(!visibleIds.has(actor.actor_id))folded.set(actor.zone_id,(Number(folded.get(actor.zone_id))||0)+1);const nextActorIds=new Set(),occupancy=new Map();for(const actor of actors){if(nextActorIds.has(actor.actor_id)||occupancy.has(actor.station_id)){folded.set(actor.zone_id,(Number(folded.get(actor.zone_id))||0)+1);continue;}nextActorIds.add(actor.actor_id);occupancy.set(actor.station_id,actor.actor_id);const existing=state.scene.actors.get(actor.actor_id);if(existing){const stationChanged=existing.actor.station_id!==actor.station_id;if(stationChanged&&!direct&&!document.hidden&&!state.scene.reducedMotion){if(!moveSceneActor(existing,actor,mobile)){cancelSceneMovement(actor.actor_id,false);updateSceneActorNode(existing.node,actor);existing.actor=actor;}}else if(state.scene.movements.has(actor.actor_id)&&!direct){moveSceneActor(existing,actor,mobile);}else{cancelSceneMovement(actor.actor_id,false);updateSceneActorNode(existing.node,actor);existing.actor=actor;}}else{const node=sceneActorNode(actor);layer.append(node);state.scene.actors.set(actor.actor_id,{actor,node});}}for(const [actorId,entry] of state.scene.actors)if(!nextActorIds.has(actorId)){cancelSceneMovement(actorId,false);clearTimeout(state.scene.eventTimers.get(actorId));state.scene.eventTimers.delete(actorId);entry.node.remove();state.scene.actors.delete(actorId);if(state.scene.selectedActorId===actorId)state.scene.selectedActorId=null;}state.scene.occupancy=occupancy;for(const summary of layer.querySelectorAll(".scene-team-summary"))summary.remove();for(const [zone,count] of folded)if(Number(count)>0)layer.append(sceneTeamSummaryNode(zone,Number(count)));reconcileSceneDevices(visualProject);syncOfficeSceneRelations(visualProject);syncOfficeSceneProcess(data,visualProject,actors,direct);syncOfficeSceneReports(data,visualProject,actors);empty.hidden=nextActorIds.size>0;$("officeSceneStatus").textContent="智能体 "+nextActorIds.size+" · 等待老板 "+project.counts.waiting_user+" · 异常 "+project.counts.incident+" · 浏览器操作 "+project.resource_summary.browsers;setOfficeScenePaused(state.scene.reducedMotion||document.hidden);let preferred="scene";try{preferred=localStorage.getItem("codexpro.office.view")||"scene";}catch{}setOfficeView(preferred,false);applySceneMobileFocus(actors);}
      function setOfficeScenePaused(paused){const shell=$("officeSceneShell");if(shell)shell.classList.toggle("scene-paused",Boolean(paused));setOfficeProcessPaused(Boolean(paused));setOfficeSceneExperiencePaused(Boolean(paused));if(paused)cancelAllSceneMovements(true);}
      function destroyOfficeScene(){destroyOfficeProcessRuntime();cancelAllSceneMovements(false);for(const controller of state.scene.reportControllers.values())controller.abort();for(const timer of state.scene.eventTimers.values())clearTimeout(timer);const layer=$("officeActorLayer");if(layer)layer.replaceChildren();state.scene.actors.clear();state.scene.devices.clear();state.scene.occupancy.clear();state.scene.reportControllers.clear();state.scene.reportCursors.clear();state.scene.eventTimers.clear();state.scene.feedKeys.length=0;const feed=$("officeSceneLiveFeed");if(feed)feed.innerHTML='<span class="scene-live-feed-empty">等待新的重要进展…</span>';state.scene.selectedActorId=null;state.scene.projectId=null;$("officeSceneShell")?.classList.remove("route-visible","moving");for(const device of document.querySelectorAll(".scene-device[data-state]")){device.dataset.state="inactive";device.dataset.objectiveKey="";device.removeAttribute("title");}}
      function installOfficeSceneSurface(){configureOfficeSceneFeature(officeSceneFeature);installOfficeProcessRuntime();populateSceneFocus();const switcher=$("officeViewSwitch"),focus=$("officeSceneZoneFocus");if(!switcher||state.scene.listenersInstalled)return;const listen=(target,type,listener,options)=>{target.addEventListener(type,listener,options);state.scene.listenerCleanups.push(()=>target.removeEventListener(type,listener,options));};installOfficeSceneExperienceControls(listen);for(const button of switcher.querySelectorAll("[data-office-view]"))listen(button,"click",()=>setOfficeView(button.dataset.officeView));if(focus)listen(focus,"change",()=>{state.scene.focusZone=focus.value;applySceneMobileFocus([...state.scene.actors.values()].map((entry)=>entry.actor));});for(const device of document.querySelectorAll(".scene-device")){const open=()=>{if(device.dataset.objectiveKey)openDrawer(device.dataset.objectiveKey,true);};listen(device,"click",open);listen(device,"keydown",(event)=>{if(event.key==="Enter"||event.key===" "){event.preventDefault();open();}});}listen(window,"resize",()=>{if(state.data)updateOfficeScene(state.data,state.viewMode,true);else applySceneMobileFocus([...state.scene.actors.values()].map((entry)=>entry.actor));},{passive:true});state.scene.listenersInstalled=true;}
      function uninstallOfficeSceneSurface(){for(const cleanup of state.scene.listenerCleanups.splice(0))cleanup();state.scene.listenersInstalled=false;}
`;
